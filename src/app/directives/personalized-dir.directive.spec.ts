import { PersonalizedDirDirective } from './personalized-dir.directive';

describe('PersonalizedDirDirective', () => {
  it('should create an instance', () => {
    const directive = new PersonalizedDirDirective();
    expect(directive).toBeTruthy();
  });
});
